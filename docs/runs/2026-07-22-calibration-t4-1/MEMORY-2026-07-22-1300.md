# Memory — Runtime intake provenance v2

Date: 2026-07-22 13:09 local.

The previous published commit `f0a1dd4388eaa0a035a6949c471436b2d20a4e71` is green in Desktop CI `29940061181` across all three producer and all three receiver jobs. The current candidate closes a remaining supply-chain gap: intake v1 accepted arbitrary operator files and only recorded their new hashes. Intake v2 now requires every immutable source package, verifies its locked hash/size, binds companion libraries to package hashes, verifies direct models and pinned first-party binaries, and preserves compact provenance in the proposed runtime manifest.

Files changed: `calibrationRuntime.ts`, `calibrationRuntimeProvisioning.ts`, preparation CLI, runtime/provisioning tests, asset guide and RTX 5090 gate. Decisions: no runtime download; v1 rejected explicitly; archives verified but not copied; no automatic approval. Local proof: 25 files / 206 tests, typecheck, build, source isolation, asset audit, reproducible three-target telemetry and zero verification temporaries. Next: publish coherent commit, require native CI 3×3, then obtain approved packages and perform physical CPU/GPU qualification. The overall goal remains incomplete until those physical gates pass.
