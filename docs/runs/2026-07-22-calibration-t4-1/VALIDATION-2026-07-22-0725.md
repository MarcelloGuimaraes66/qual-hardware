# Validation — Calibration T4.1

Status: local gates green; native/physical pre-commit matrix pending.

## Local evidence on macOS arm64

- Baseline before T4.1: 21 test files / 154 tests passed.
- Final targeted critical set: 5 test files / 59 tests passed.
- Final complete suite: 24 test files / 183 tests passed after telemetry and cancellation fixes.
- TypeScript application and server typecheck: passed.
- Production Vite/server build: passed.
- Packaged Electron smoke: two final consecutive runs passed after the telemetry/cancellation rebuild, including packaged telemetry integrity, measured `thermal_sustain`, internal diagnostic completion, durable result before 100%, exact-session zero-byte cleanup, immediate cancellation cleanup, shutdown cleanup, and retained compact evidence. A failing run exposed that a deliberately terminated FFmpeg process was misclassified as hardware failure; cancellation dominance in both worker and service fixed it before the successful reruns.
- Source-authority audit: passed with `externalSourceAccess:false`; contract hashes match the committed authority records.
- Telemetry build verification: Go 1.26.5, 11 source files / 21,334 bytes, source SHA-256 `81bdf9a4cbcd967996ab9985e0d4fab6bcafcb1fe4459a80bb36aaa941d1351f`; two identical builds per target; local macOS execution returned `thermalThrottling:measured`; build temporaries ended at zero bytes.
- Telemetry binary SHA-256: macOS `5a62d169acb3fe02221c8685547b8a0a4aeb57e75c0d3f4bc31d81b32fabf62e`, Windows `57a92ef8cd710595287dbc9e4d15a57a69716ed70fc38e613e2feb0ee649200b`, Linux `cb31f34f09f82c94774dacf08960a306e9435e5c014576b68a43620ce49a3949`.
- CI exchange rehearsal: three separately signed diagnostic-only `.qhcal` packages (Windows, Linux and macOS identities) were generated, parsed, signature-checked and consolidated into one three-item `.qhcalset` on macOS; all test-owned output was then removed. Hosted receiver jobs for all three OS are defined but cannot execute before a candidate commit/push.
- Workflow validation: `desktop-ci.yml` and `desktop-release.yml` parsed successfully; both install Go from `tools/telemetry-probe/go.mod` and execute probe/source/asset gates. `desktop-ci` adds three native producers and three native receivers.
- Asset-source audit: inventory/schema/repository hash passed for all nine IDs and three targets; `readyForProvisioning:false` is the required fail-closed result. No external asset was downloaded. Projected peaks are about 9.64–9.65 GB; the final audit observed about 12.62 GB free and a 53,687,091,200-byte reserve.
- Dependency audit: zero high-or-higher vulnerabilities.
- `git diff --check`: passed.
- macOS installer: `release-telemetry-cancel-fix-20260722-1228/Qual-Hardware-0.1.0-macos-arm64.dmg` generated separately and `hdiutil verify` passed.
- Final DMG SHA-256: `d861543df64bb9cdc72e91f3daae6781a9921a148d5806ceb15ae6a7b3d8ef37`.
- Packaged source-lock SHA-256: `444999ebbca2cf0a1937e20a597e33cbd5a82a8f07c8875161817cb64a9e3e4c`, equal to the runtime-manifest reference.
- Signing: intentionally absent because `identity: null`; notarization/signing remains a release gate, not a hidden success claim.

## Requirements covered by automated/adversarial tests

- Monotonic overall/phase percentage, ETA adjustment, long-phase 99% cap, and terminal 100% cleanup gate.
- Fifteen-second persistence cadence, phase-boundary persistence, and clock-change-resistant monotonic timing.
- 15% disk reserve clamped to 10–50 GiB, phase peak admission, two-second pressure checks, FFmpeg byte ceiling, bounded logs, and commit-before-reclaim.
- Success, failure, cancellation, cleanup retry, worker crash, application shutdown, and restart cleanup recovery.
- Mutable-file hash synchronization between the persisted and live phase manifests before checkpoint reclamation; this closes the FFmpeg predeclared-file race found by the final packaged smoke.
- Ordered worker-message persistence and terminal progress rejection; this closes the immediate-cancellation state resurrection found by the source-lock packaged smoke.
- Strict source-lock inventory, HTTPS host allowlist, immutable release/revision selectors, SHA-256/size validation, unique-source peak calculation, no-download audit, pre-mutation disk refusal, and runtime-manifest source-lock hash verification.
- Traversal, symlink, invalid UUID, altered manifest, external path, unregistered file, locked file, and controlled-root preservation.
- Checkpoint compatibility across hardware, OS/version, driver, workload/build, kernel/manifest, model, policy, and application version.
- Real gzip, canonical ordering, Ed25519 signature/digest validation, first-use trust, revocation, duplicates, ID collisions, compression bombs, 20/50 MiB and 250/500 MiB limits, and no source-session requirement.
- One `.qhcalset` containing ten signed machine fixtures, collector import, logical consolidation, and collector re-export with the original device signatures intact.
- Exact evidence priority, lowest safe exact capacity, distinct-configuration extrapolation gate, benchmark-only planning status, and catalog-triggered recalculation.
- SQLite v9 preservation, extension v2, verified pre-migration backup, rollback on migration failure, append-only import/checkpoint/session evidence, and atomic run/prediction commit.
- Loopback API, renderer sandbox/CSP, forbidden calibration data rejection, and no outbound Perceptrum/OpenAI path in the autonomous kernel.

## Pending mandatory evidence

- Approved platform binaries/outputs for the pinned FFmpeg, ffprobe, MediaMTX, llama-server and Qwen candidates; telemetry-probe legal/SBOM review and physical sensor approval; final approved-manifest hashes for all targets.
- Native packaged smoke and full physical run on Windows 11 x64.
- Native packaged smoke and full physical run on Ubuntu 24.04 x64.
- Full physical three-repetition run on macOS arm64 with approved assets.
- Cross-platform package exchange using the three physical results, zero-egress capture, zero session-temporary bytes, and final native installer digests.
- Hosted execution of the new Windows/macOS/Ubuntu telemetry and three-producer × three-receiver exchange matrices after the pre-commit physical gate authorizes a candidate commit.

These pending items are explicit hard gates from the supplied plan. They prevent a completion claim and prevent the candidate commit; they do not represent a known failing local code test.
