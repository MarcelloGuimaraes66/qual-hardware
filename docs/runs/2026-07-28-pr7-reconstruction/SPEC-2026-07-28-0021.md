# Especificação — PR #7 reconstruído

## Mudança funcional

Nenhuma. O comportamento de produção da `main` permanece inalterado.

## Testes acrescentados

### Entradas Windows explícitas

- `documentsDirectory = C:\Users\test\Docs` deve produzir
  `C:\Users\test\Docs\Qual Hardware\Calibracoes`.
- override `D:\Calibrations` deve produzir
  `D:\Calibrations\Qual Hardware\Calibracoes`.

### Home Windows injetado

- mockar `node:child_process.execFile`;
- carregar novamente o módulo;
- resolver com `platform: "win32"` e `home: C:\Users\test`;
- confirmar o caminho Windows determinístico;
- confirmar que `execFile` não foi chamado;
- remover o mock após o teste.

## Aceitação

- teste direcionado aprovado;
- suíte completa, typecheck e build aprovados;
- diff contra `main` limitado aos testes e documentos desta reconstrução;
- CI do PR aprovada antes do merge.
