# Código — reconstrução do PR #7

## Arquivos alterados

- `tests/calibration-sessions.test.ts`

## Arquivos criados

- documentos desta execução em `docs/runs/2026-07-28-pr7-reconstruction`.

## Decisão

O código de produção não foi alterado. A implementação atual de
`resolveCalibrationDirectory` já seleciona `win32`/`posix`, respeita
`documentsDirectory`, override de ambiente, diretório exato do desktop e evita
consulta à pasta real quando plataforma ou home são injetados.

Foram acrescentados somente:

- teste das duas entradas explícitas Windows;
- teste com mock de `node:child_process.execFile` comprovando ausência de shell.

Contratos v1/v2, fixtures, UI e runtime do commit 0.2.0 não foram restaurados.
