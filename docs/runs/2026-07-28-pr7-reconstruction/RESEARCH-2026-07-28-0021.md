# Pesquisa — conteúdo ainda útil do PR #7

## Commit `63320a6`

Introduziu seleção explícita de `win32` ou `posix` em
`resolveCalibrationDirectory`. A implementação atual já contém esse
comportamento e ainda acrescenta suporte ao diretório de evidências fornecido
pelo desktop.

Conclusão: código superado; não reaplicar.

## Commit `cfdae3d`

Acrescentou duas proteções de regressão:

1. `documentsDirectory` e `QUAL_HARDWARE_CALIBRATION_DOCUMENTS_DIR` precisam
   manter semântica Windows mesmo quando o teste roda em outro sistema;
2. injetar `platform: "win32"` e `home` não pode consultar o Documents real da
   máquina pelo PowerShell.

Conclusão: os comportamentos existem, mas os testes específicos não estão na
suíte atual; reaplicar somente esses testes.

## Commit `3b0b442`

Trazia 36 arquivos da linha 0.2.0: contratos v1/v2, fixtures antigas, alterações
de UI, servidor, desktop e empacotamento. A `main` atual usa contratos e
calibração posteriores, logger e launcher evoluídos, Qwen certificado e RTSP
funcional.

Conclusão: reaplicar esse commit causaria regressão e foi rejeitado.

## Evidência de preservação

O head antigo foi enviado sem alteração para
`codex/archive-pr7-windows-qual-hardware-20260728`.
