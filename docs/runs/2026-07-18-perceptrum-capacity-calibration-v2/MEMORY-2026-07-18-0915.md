# Memory — Perceptrum capacity calibration v2

Status: implementation and macOS validation complete.

Continuity source: this run directory and Archon run
`a68f480b9bbf2908369f48e600b02150`. External Obsidian memory was unavailable.

Preserve these decisions:

- RTSP read/decode FPS and AiQ inference FPS are separate quantities.
- AiQ/Qwen Core currently uses 1 inference FPS in 60-second cycles.
- Physical calibration never claims more cameras than it actually ran.
- Extrapolation is per stage, takes the conservative anchor minimum and applies
  20/30/40% reserves; architecture/backend gaps are reference-only.
- Public data enters only as curated signed snapshots, never live scraping.
- Existing databases are evolved additively and never moved or recreated.
- macOS has one valid physical anchor. More anchors and a signed evidence catalog
  are data-collection gates, not reasons to weaken the safety classification.

Perceptrum counterpart: Archon run `07a4e2a62bc82abac83f017735eb182c` on branch
`archon/local-calibration-extrapolation-v2`.
