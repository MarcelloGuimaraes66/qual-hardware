# Planning — Perceptrum capacity calibration v2

- Date: 2026-07-18 09:15 America/Manaus
- Risk: T4; targeted redesign.
- Request: replace the remote/Windows-only benchmark manifest with local offline calibration, import 3–4 physical anchors, conservatively extrapolate untested hardware, expose at least six diverse cost-ordered candidates, and preserve native packaging for macOS, Windows, and Ubuntu.
- Baseline: commit `0d11a20`; 40 tests and production build pass. Current workload is `perceptrum-workload/1.1.0`; benchmark manifest posts to a random local origin; SQLite schema is v1; model FPS accepts 1–10 although Perceptrum inference clamps effective video sampling.

## Controls

- Facts: Electron is already cross-platform; catalog has 14 complete systems; current benchmark evidence validates only exact CPU/GPU; current storage demand is intentionally zeroed; `replaceCatalog` deletes catalog rows.
- Invariants: no OpenAI/external provider in calibration; no media/credentials in evidence; database path, app identity, scenario compatibility, HTTP contracts and existing reports remain readable; no destructive database or filesystem operation.
- Blast radius: calibration contracts/engine/store/API/UI/reports, additive SQLite tables, signed evidence snapshots, tests and documentation. Perceptrum runner is implemented in its own repository/worktree.
- Out of scope: purchasing machines, claiming physical Windows/Ubuntu validation, live web scraping, auto-update, database relocation, or deletion of historical benchmark v1 data.
- Change budget: targeted redesign with additive compatibility adapters.

## Strategy and acceptance

1. Add v2 workload, calibration, evidence and prediction contracts.
2. Store calibration/evidence/predictions append-only and upsert catalog snapshots without deleting old rows.
3. Predict each bottleneck by stage-specific anchor ratios, choose the conservative minimum, apply 20/30/40% reserves and demote low coverage.
4. Replace the manifest action with a calibration center for plan export, result import, evidence update and recalculation.
5. Restore disk/network/full-pipeline constraints and separate RTSP FPS from effective AiQ FPS (1–5).
6. Validate formulas, provenance, offline policy, legacy scenarios, schema preservation, API/reports, desktop packaging and packaged smoke.

Rollback: revert the branch. Additive schema objects and existing rows remain valid; no data migration or deletion is allowed.
