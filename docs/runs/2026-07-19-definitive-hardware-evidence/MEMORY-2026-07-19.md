# Memória — dimensionamento definitivo

## Estado local

- Branch: `archon/refactor-definitive-hardware-evidence`.
- Base: `07150a5`.
- Alterações permanecem sem commit por solicitação do usuário.
- Executável macOS: `release/mac-arm64/Qual Hardware.app`.
- DMG: `release/Qual-Hardware-0.1.0-macos-arm64.dmg`.

## Decisões permanentes

- Nenhum score genérico aprova compra.
- Cada estágio do Perceptrum tem observação, unidade, perfil e âncora próprios.
- Capacidade final é o menor limite após erro empírico e reserva.
- Somente `validated_local` ou `extrapolated_high` pode ser `eligible`.
- Ausência de evidência resulta em `blocked`, sem câmera comercial inventada.
- Para 64–256 câmeras, o desenho recomendado inclui N+1.
- Atualizações públicas são append-only, assinadas e ativadas atomicamente.

## Continuidade

1. Preservar este worktree e não misturar com o checkout principal.
2. Obter 3–5 arquivos `.qhcal.json` completos em hardwares distintos.
3. Executar o publicador em dry-run, revisar licenças/proveniência e publicar o primeiro bundle assinado somente após autorização.
4. Executar os workflows em Windows 11 e Ubuntu 24.04 e depois homologar sensores/drivers nas máquinas físicas.
5. Repetir leave-one-out; liberar aquisição apenas se nenhuma âncora for superestimada após as margens.

Archon estava sem créditos; planejamento, implementação, validação e memória foram mantidos manualmente no mesmo padrão T4.

## Ambiente local preservado

O macOS carrega `~/.config/aiquimist/toolchain-path.zsh` por `~/.zprofile` e `~/.zshrc`. O fragmento promove apenas diretórios de ferramentas existentes, preserva todas as demais entradas e mantém Node.js 24 como runtime padrão dos projetos. Bibliotecas C++ são anunciadas por `CMAKE_PREFIX_PATH` e `PKG_CONFIG_PATH`; não foi definido `DYLD_LIBRARY_PATH` global.
