# Code record

- Removed standalone web server entrypoint and hosting artifacts; `npm start` and `npm run dev` now open Electron.
- Extended scenario/catalog types with platform, laptop/mini-PC form factors, exact-template constraint, memory architecture and conservative sustained limits.
- Added three ASUS laptop tiers and four Apple Silicon configurations with technical/pricing sources.
- Updated the optimizer, UI, catalog manager and reports for shared/unified memory, platform compatibility and exact-machine capacity.
- PDF wrapping now hard-breaks long source URLs to prevent clipping.

