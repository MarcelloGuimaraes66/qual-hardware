# Specification

- Add form factors `laptop` and `mini_pc`, target platform selection and an optional exact-hardware constraint.
- Automatic mode considers Windows/Ubuntu computers; Apple remains explicit until a matching build exists.
- Selecting integrated hardware switches scenario decode to CPU and preserves the exact template constraint.
- Display estimated total camera capacity for the selected Agent profile.
- Apply transparent conservative sustained/FFmpeg limits to mobile and unbenchmarked SoC hardware.
- Show the complete active catalog in the always-visible hardware update manager.

