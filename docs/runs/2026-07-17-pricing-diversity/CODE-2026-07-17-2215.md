# Code record

- Coordinated primary selection prevents unconstrained minimum/recommended/N+1 proposals from reusing one hardware template when compatible non-downgrade choices exist.
- `referenceCosts.ts` supplies dated, componentized planning estimates when live signed quotes are absent.
- PDF, XLSX, JSON and the React result view expose basis, range, component/node/project totals and purchase-quotation status.
- SQLite bundled-catalog startup now upserts every bundled template so an old partial local catalog cannot masquerade as the current version.

