# Planning

- Project: standalone **Qual Hardware**, repository `C:\dev\perceptrum_desktop_aspp\qual-hardware`.
- Process authority: global Archon configuration. No repository-local `.archon` memory or workflow exists.
- Risk: T4 because the change affects infrastructure selection and cost logic.
- Change budget: targeted redesign of recommendation coordination and report pricing only.
- Excluded: Perceptrum runtime, camera demand equations, benchmark validation, SQLite scenario schema and deployments.

## Facts

- The supplied 24-camera PDF selects `ws-rtx5090-9950x` for minimum, recommended and N+1; only node count/headroom changes.
- `buildRecommendations` selects each policy independently and does not exclude an already selected hardware template.
- The bundled price seed is empty. Therefore every report prints only `quotation_required`.
- The BOM contains technical specifications but no numeric component-cost columns.

## Invariants

- Every selected design must still satisfy all CPU, RAM, VRAM, AiQ-slot, decoder, process and network limits.
- Minimum retains 15% headroom; recommended and N+1 retain 30%; N+1 retains a reserve node.
- Actual quotes take precedence over reference estimates.
- Reference values are visibly identified as estimates, include date/sources, exclude freight/taxes, and still require a purchase quotation.
- Imported/signed catalogs and existing SQLite projects remain readable.

## Platform matrix

- Windows desktop: implementation and packaging target; full validation required.
- macOS/Linux desktop: not currently packaged.
- Ubuntu rack hardware remains a recommendation target only; no Perceptrum deployment is changed.

